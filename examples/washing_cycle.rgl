module Wash
  (
   input  push,
   output fill,
   output empty
   );

   if "{true} * {push} {! fill} 5" then "{fill}";
   never "{!push} 5 {fill}";
   if "{fill}" then "{!empty} {empty}";

endmodule
