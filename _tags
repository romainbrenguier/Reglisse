<*.ml>: pp(camlp4o)
<smv.*> or <translation.*> or <coalition.*>: use_ocaml-aiger
<attractor.*> or <parity.*> or <admissibility.*> or <simulation.*> or <weakMuller.*> or <weakMullerEquilibria.*> or <weakMullerNash.*> or <coBuchi.*> or <determinization.*> or <psl.*> or <stateFormula.*> or <region.*>: use_ocaml-aiger, use_ocaml-cudd, use_speculog
<parametricModule.*> or <speculogModules.*> or <environment.*>: use_ocaml-aiger
<regularExpression.*> or <limitEquilibria.*> or <speculog.*> or <limitNash.*> : use_ocaml-aiger, use_ocaml-cudd, use_dynlink
<speculogParser.*> : use_ocaml-aiger, use_dynlink
<anzu2ltl.*> : pp(camlp4o), str
<reglisse.*> : pp(camlp4o), use_ocaml-aiger, use_ocaml-cudd, use_speculog